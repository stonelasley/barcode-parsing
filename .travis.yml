sudo: required
dist: focal
git:
  depth: 1
language: node_js
node_js:
  - lts/* 
cache:
  directories:
    - node_modules
matrix:
  fast_finish: true
install:
  - yarn 
script:
  - yarn test 
after_success:
  - yarn coverage 
  - yarn release
